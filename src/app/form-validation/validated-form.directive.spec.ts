import { ValidatedFormDirective } from './validated-form.directive';

describe('ValidatedFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatedFormDirective();
    expect(directive).toBeTruthy();
  });
});
